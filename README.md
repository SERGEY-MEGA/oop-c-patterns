# OOP Design Patterns in C

Учебный проект по реализации паттернов проектирования на языке C
с эмуляцией принципов ООП через структуры и указатели на функции.

🛠 Реализованные паттерны
📦 Порождающие (Creational)
Овечают за гибкое создание объектов без лишних зависимостей.
• Factory Method — создание объектов через специальный интерфейс.
• Singleton — гарантирует наличие только одного экземпляра структуры.
• Builder — пошаговое создание сложных объектов.
• Abstract Factory — производство целых семейств связанных объектов.
🧱 Структурные (Structural)
Помогают строить связи между компонентами, делая систему гибкой.
• Adapter — соединяет несовместимые интерфейсы.
• Facade — предоставляет простой интерфейс к сложной системе.
• Bridge — разделяет абстракцию и реализацию.
• Decorator — динамически добавляет новые обязанности объекту.
• Composite — организует объекты в древовидные структуры.
• Proxy — контролирует доступ к объекту.
• Flyweight — экономит память, разделяя общие данные объектов.
💬 Поведенческие (Behavioral)
Фокусируются на алгоритмах и распределении обязанностей между структурами.
• Strategy — позволяет менять алгоритм «на лету».
• Observer — механизм оповещения других объектов об изменениях.
• Template Method — определяет скелет алгоритма, перекладывая детали на наследников.
• Command — превращает запрос в объект.
• Chain of Responsibility — передает запрос по цепочке обработчиков.
• Iterator — последовательный обход элементов коллекции.
• State — изменение поведения объекта в зависимости от его состояния.
• Mediator — упрощает общение между множеством объектов.
• Memento — сохраняет и восстанавливает состояние объекта.
• Interpreter — определяет грамматику для простых языков.
• Visitor — добавляет операции в классы, не изменяя их.
📚 Дополнительно
• Stack — пример реализации базовой структуры данных на C.
• Функциональные указатели — активное использование void (*func)() для реализации «методов» внутри структур.


Указатели на функции
Это очень ценная учебная работа, так как показывает, как можно применять ООП-паттерны в языке, который изначально не предназначен для объектно-ориентированного программирования.
Команда сборки clang *.c -o app -Wall -Wextra -lm компилирует все реализации паттернов в одно приложение, демонстрирующее их работу.

## Как собрать и запустить

# 1. Перейдите в папку, где хотите разместить проект
cd ~/Documents  # или другую удобную папку

# 2. Склонируйте репозиторий
git clone https://github.com/SERGEY-MEGA/oop-c-patterns.git

# 3. Перейдите в папку проекта
cd oop-c-patterns

# 4. Посмотрите какие файлы есть
ls -la

# 5. Скомпилируйте (если есть .c файлы)
clang *.c -o app -Wall -Wextra -lm

# 6. Запустите
./app


```bash
make clean
clang *.c -o app -Wall -Wextra -lm
./app

